name: TEMPORARY DEBUG - PLEASE RUN THIS

on:
  push:
    branches:
      - main

jobs:
  debug:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
      
      - name: ‼️‼️ FIND AND COPY THE OUTPUT OF THIS STEP ‼️‼️
        run: |
          cd ./template/app
          npm install
          curl -sSL https://get.wasp-lang.dev/installer.sh | sh
          /home/runner/.local/bin/wasp build
          echo "############################################################"
          echo "### LÜTFEN BU BAŞLIKTAN SONRAKİ LİSTENİN TAMAMINI KOPYALAYIN ###"
          echo "############################################################"
          cd ../../
          ls -R
