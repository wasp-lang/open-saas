# Wasp Configuration

1. read [`../../../app/main.wasp`](../../../app/main.wasp) for existing patterns
2. add feature section with region comments

Key points:
- list ALL entities in `entities: [...]` array
- set `authRequired: true` for authenticated pages
