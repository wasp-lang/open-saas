---
description: Shows the Wasp plugin's available features, commands, and skills.
---

Explicitly invoke the `plugin-help` skill in the Claude Code session for the user.

Example:
```
Run the `plugin-help` skill.
```
