# ===== PayTheFly Configuration =====
# PayTheFly is a crypto payment processor supporting BSC and TRON chains.
# Get your project credentials at https://pro.paythefly.com

# Your PayTheFly project ID (found in the PayTheFly Pro dashboard)
PAYTHEFLY_PROJECT_ID=your-project-id

# Your PayTheFly project key (used for webhook HMAC verification)
PAYTHEFLY_PROJECT_KEY=your-project-key

# The private key used for EIP-712 signature generation.
# ⚠️ SECURITY: This should be a dedicated signing key, NOT your main wallet.
# Generate a new wallet specifically for signing payment requests.
PAYTHEFLY_SIGNER_PRIVATE_KEY=0x...

# Chain ID: 56 for BSC (BNB Chain), 728126428 for TRON
PAYTHEFLY_CHAIN_ID=56

# The PayTheFly verifying contract address for EIP-712 domain
PAYTHEFLY_VERIFYING_CONTRACT=0x...

# (Optional) Token address for payments. Defaults to native token (address(0)).
# Set this to a specific token contract address (e.g., USDT, USDC) if needed.
# PAYTHEFLY_TOKEN_ADDRESS=0x0000000000000000000000000000000000000000

# ===== Plan Prices (in token units) =====
# These must match the plan IDs configured in PAYMENTS_*_PLAN_ID env vars.
# For BSC (18 decimals): use BNB amounts like "0.05"
# For TRON (6 decimals): use TRX amounts like "10"

# Price for the Hobby subscription plan
PAYTHEFLY_PRICE_hobby_plan_id=0.03

# Price for the Pro subscription plan
PAYTHEFLY_PRICE_pro_plan_id=0.06

# Price for the 10 Credits one-time purchase
PAYTHEFLY_PRICE_credits_plan_id=0.03
