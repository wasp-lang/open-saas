--- template/app/src/payment/PricingPage.tsx
+++ opensaas-sh/app/src/payment/PricingPage.tsx
@@ -39,6 +39,7 @@
 
 const PricingPage = () => {
   const [isPaymentLoading, setIsPaymentLoading] = useState<boolean>(false);
+  const [isStripe, setIsStripe] = useState<boolean>(true);
 
   const { data: user, isLoading: isUserLoading } = useAuth();
 
@@ -51,8 +52,12 @@
     }
     try {
       setIsPaymentLoading(true);
-      // const checkoutResults = await generateStripeCheckoutSession(paymentPlanId);
-      const checkoutResults = await generateLemonSqueezyCheckoutSession(paymentPlanId);
+      let checkoutResults;
+      if (isStripe) {
+        checkoutResults = await generateStripeCheckoutSession(paymentPlanId);
+      } else {
+        checkoutResults = await generateLemonSqueezyCheckoutSession(paymentPlanId);
+      }
       if (checkoutResults?.sessionUrl) {
         window.open(checkoutResults.sessionUrl, '_self');
       } else {
@@ -61,7 +66,7 @@
     } catch (error) {
       console.error(error);
       setIsPaymentLoading(false);
-    } 
+    }
   }
 
   const handleCustomerPortalClick = () => {
@@ -70,8 +75,7 @@
       return;
     }
     const schema = z.string().url();
-    // const customerPortalUrl = schema.safeParse(import.meta.env.REACT_APP_STRIPE_CUSTOMER_PORTAL);
-    const customerPortalUrl = schema.safeParse(user.lemonSqueezyCustomerPortalUrl);
+    const customerPortalUrl = schema.safeParse(isStripe ? import.meta.env.REACT_APP_STRIPE_CUSTOMER_PORTAL : user.lemonSqueezyCustomerPortalUrl);
     if (customerPortalUrl.success) {
       window.open(customerPortalUrl.data, '_self');
     } else {
@@ -87,10 +91,15 @@
             Pick your <span className='text-yellow-500'>pricing</span>
           </h2>
         </div>
-        <p className='mx-auto mt-6 max-w-2xl text-center text-lg leading-8 text-gray-600 dark:text-white'>
-          Choose between Stripe and LemonSqueezy as your payment provider. Just add your Product IDs! Try it out below with test credit card number <br />
-          <span className='px-2 py-1 bg-gray-100 rounded-md text-gray-500'>4242 4242 4242 4242 4242</span>
-        </p>
+        <div className='mx-auto mt-6 max-w-2xl text-center text-lg leading-8 text-gray-600 dark:text-white flex flex-wrap items-center justify-center space-x-2'>
+          <p>Choose between</p>
+          <PaymentProviderSelector isStripe={isStripe} setIsStripe={setIsStripe} />
+          <p>
+            as your payment provider. Just add your Product IDs! Try them out below with test credit card number <br />
+            <span className='px-2 py-1 bg-gray-100 rounded-md text-gray-500'>4242 4242 4242 4242 4242</span>
+          </p>
+        </div>
+
         <div className='isolate mx-auto mt-16 grid max-w-md grid-cols-1 gap-y-8 lg:gap-x-8 sm:mt-20 lg:mx-0 lg:max-w-none lg:grid-cols-3'>
           {Object.values(PaymentPlanId).map((planId) => (
             <div
@@ -153,11 +162,11 @@
                     {
                       'opacity-50 cursor-wait': isPaymentLoading,
                     },
-                    'mt-8 block rounded-md py-2 px-3 text-center text-sm dark:text-white font-semibold leading-6 focus-visible:outline focus-visible:outline-2 focus-visible:outline-yellow-400',
+                    'mt-8 block rounded-md py-2 px-3 text-center text-sm dark:text-white font-semibold leading-6 focus-visible:outline focus-visible:outline-2 focus-visible:outline-yellow-400'
                   )}
                   disabled={isPaymentLoading}
                 >
-                  {!!user ? 'Buy plan' : 'Log in to buy plan'}
+                  {!!user ? `Buy via ${isStripe ? 'Stripe' : 'Lemon Squeezy'}` : 'Log in to buy plan'}
                 </button>
               )}
             </div>
@@ -168,4 +177,32 @@
   );
 };
 
-export default PricingPage;
\ No newline at end of file
+export default PricingPage;
+
+function PaymentProviderSelector({ isStripe, setIsStripe }: { isStripe: boolean; setIsStripe: (isStripe: boolean) => void }) {
+  return (
+    <div className='flex items-center justify-center space-x-2'>
+      <label className='relative inline-flex items-center cursor-pointer'>
+        <input
+          type='checkbox'
+          value=''
+          className='ml-1 form-checkbox bg-gray-100 checked:bg-purple-400 rounded border-purple-400 duration-200 ease-in-out hover:bg-purple-400 hover:checked:bg-purple-600 focus:ring focus:ring-purple-300 focus:checked:bg-purple-400 focus:ring-opacity-50'
+          checked={isStripe}
+          onChange={() => setIsStripe(true)}
+        />
+        <span className={cn('ml-2 text-purple-400', { 'drop-shadow-md': isStripe })}>Stripe</span>
+      </label>
+      <p>or</p>
+      <label className='relative inline-flex items-center cursor-pointer'>
+        <input
+          type='checkbox'
+          value=''
+          className='ml-1 form-checkbox bg-gray-100 checked:bg-yellow-400 rounded border-yellow-400 duration-200 ease-in-out hover:bg-yellow-400 hover:checked:bg-yellow-600 focus:ring focus:ring-yellow-300 focus:checked:bg-yellow-400 focus:ring-opacity-50'
+          checked={!isStripe}
+          onChange={() => setIsStripe(false)}
+        />
+        <span className={cn('ml-2 text-yellow-500 ', { 'drop-shadow-md': !isStripe })}>Lemon Squeezy</span>
+      </label>
+    </div>
+  );
+}
